{
  "packageManager": "bun@1.0.0",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "d": "turbo run dev --no-cache",
    "dev": "turbo run dev --no-cache",
    "build": "turbo run build"
  }
}
